#include<iostream>
#include<conio.h>

using namespace std;

int main()

{
    int n, m, a[10], b[10];
    cout<<"Enter the size of first set: ";
    cin>>n;
    cout<<"Enter the element of set:\n";
    for(int i=0; i<n; i++)
    {
        cin>>a[i];
    }
    cout<<"Enter the size of second set: ";
    cin>>m;
    cout<<"Enter the element of set:\n";
    for(int i=0; i<m; i++)
    {
        cin>>b[i];
    }
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            
        }
        
    }
    
    return 0;
}